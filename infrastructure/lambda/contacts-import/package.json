{
  "name": "contacts-import-lambda",
  "version": "1.0.0",
  "description": "Lambda functions for contacts import processing",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outdir=dist --external:pg-native",
    "watch": "npm run build -- --watch",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.450.0",
    "@aws-sdk/client-sqs": "^3.450.0",
    "papaparse": "^5.4.1",
    "pg": "^8.11.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.0",
    "@types/papaparse": "^5.3.14",
    "@types/pg": "^8.10.9",
    "esbuild": "^0.19.8",
    "typescript": "^5.3.2"
  }
}