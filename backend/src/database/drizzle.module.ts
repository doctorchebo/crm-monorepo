import { Module } from '@nestjs/common';

/**
 * Database Module
 * Manages Drizzle ORM configuration and connection
 * - Schema definitions in schema.ts
 * - Database queries and operations
 * - Migration management
 * - Database seeding
 */
@Module({
  providers: [],
  exports: [],
})
export class DatabaseModule {}
